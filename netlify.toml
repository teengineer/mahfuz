[build]
  base = "."
  command = "cd apps/web && pnpm build && cp -r .netlify dist/ && sed -i 's|../../../dist/server|../../../server|' dist/.netlify/v1/functions/server.mjs"
  publish = "apps/web/dist/client"

[build.environment]
  NODE_VERSION = "22"
